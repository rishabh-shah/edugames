{
  "name": "edugames-platform",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "build": "pnpm -r --if-present run build",
    "dev": "node ./scripts/dev.mjs",
    "lint": "pnpm -r --if-present run lint",
    "test": "pnpm -r --if-present run test",
    "test:e2e": "playwright test",
    "test:playwright": "playwright test tests/e2e/shape-match.spec.ts",
    "test:sample-game": "pnpm --filter @edugames/shape-match test && pnpm --filter @edugames/shape-match build && pnpm --filter @edugames/game-validator validate ../../games/shape-match",
    "test:integration": "pnpm -r --if-present run test:integration",
    "typecheck": "pnpm -r --if-present run typecheck",
    "verify": "pnpm lint && pnpm typecheck && pnpm test && pnpm test:integration && pnpm build && pnpm test:sample-game && pnpm test:playwright"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.2",
    "@types/node": "^24.9.1",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
