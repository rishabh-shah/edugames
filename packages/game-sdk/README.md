# Game SDK

Purpose: Shared runtime helpers for approved EduGames titles integrating with the shell bridge and platform rules.

Initial scaffold:
- `src/` holds the future browser-side SDK entry point.
- `tests/` holds lightweight workspace smoke coverage until shared tooling lands.
