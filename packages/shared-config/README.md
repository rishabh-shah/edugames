# Shared Config

Purpose: Reusable repository configuration package for linting, TypeScript, formatting, and other shared defaults.

Initial scaffold:
- `src/` holds the future shared config entry point and presets.
- `tests/` holds lightweight workspace smoke coverage until shared tooling lands.
