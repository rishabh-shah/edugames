# Game Validator

Purpose: Validation tooling for reviewed game bundles, manifests, assets, and release-time policy checks.

Initial scaffold:
- `src/` holds the future validator entry point and rule set.
- `tests/` holds lightweight workspace smoke coverage until shared tooling lands.
