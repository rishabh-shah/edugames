# Admin Web

Purpose: Internal maintainer dashboard for catalog review, approvals, audit trails, and release operations.

Current scaffold:
- `src/app/` holds the Next.js App Router shell.
- `src/components/` holds dashboard presentation components.
- `src/lib/` holds typed fixture data until the moderation/reporting API is wired in.
- `tests/` holds workspace setup and smoke coverage.
