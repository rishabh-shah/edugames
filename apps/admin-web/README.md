# Admin Web

Purpose: Internal maintainer dashboard for catalog review, approvals, audit trails, and release operations.

Initial scaffold:
- `src/` holds the future application shell and feature modules.
- `tests/` holds lightweight workspace smoke coverage until shared tooling lands.
