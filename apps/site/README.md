# Site

Purpose: Public-facing EduGames website for product information, contributor docs, and launch-ready static content.

Initial scaffold:
- `src/` holds the future site entry point and content integrations.
- `tests/` holds lightweight workspace smoke coverage until shared tooling lands.
