const lines = [
  "EduGames bootstrap dev command",
  "",
  "The runnable app stack is not wired yet.",
  "Current focus:",
  "- workspace scaffolding",
  "- test and CI foundations",
  "- backend, web, and package placeholders",
  "",
  "Use `pnpm lint`, `pnpm typecheck`, `pnpm test`, and `pnpm build` during bootstrap."
];

console.log(lines.join("\n"));
