# Worker

Purpose: Background job runner for catalog maintenance, review processing, and other async platform tasks.

Initial scaffold:
- `src/` holds the future worker entry point and job handlers.
- `tests/` holds lightweight workspace smoke coverage until shared tooling lands.
