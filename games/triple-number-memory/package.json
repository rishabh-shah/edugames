{
  "name": "@edugames/triple-number-memory",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Triple Number Memory packaged as a static EduGames HTML5 bundle.",
  "scripts": {
    "build": "node ./scripts/build.mjs",
    "test": "vitest run --config vitest.config.ts --configLoader runner",
    "lint": "node ./scripts/lint.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
